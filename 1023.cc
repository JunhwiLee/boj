#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;

    vector<vector<int>> dp1(50, vector<int>(50));
    vector<vector<int>> dp2(50, vector<int>(50));

    dp[0][0] = 1;
    dp[1][0] = 1;
    
    for(int i = 1; i<50; i++) {
        
    }
}