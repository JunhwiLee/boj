#include <bits/stdc++.h>
using namespace std;



vector<vector<int>> fibo(long long namespace){
    if(n == 1) return {{1, 1}, {1, 0}};
    if(n % 2 == 0){
        return fibo
    }
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    long long n, m;
    cin >> n >> m:

    
}